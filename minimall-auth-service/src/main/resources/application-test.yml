spring:
  datasource:
    url: jdbc:h2:mem:minimall_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  cloud:
    nacos:
      discovery:
        enabled: false

minimall:
  jwt:
    secret: test-jwt-secret-0123456789abcdef0123456789abcdef
  security:
    aes-key: pwKs8yE75abt15VzzFRV6ynHoD4I/idpBDQ8sN92fLA=