spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:merchant_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  cloud:
    nacos:
      discovery:
        enabled: false